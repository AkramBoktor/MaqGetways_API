using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.Options;
using MaqGetway.Domain.Entities;
using MaqGetway.Domain.Interfaces;
using MaqGetway.Infrastructure.Context;
using MaqGetway.Infrastructure.Repository;
using MaqGetway.Apllication.Service.Service;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection.Metadata;
using System.Text;
using System.Threading.Tasks;

namespace MaqGetwaye.Test
{
    public class JsonItemFixture
    {
       // private const string ConnectionString = @"Server=(localdb)\mssqllocaldb;Database=EFTestSample;Trusted_Connection=True";

       // DbContextOptionsBuilder options = new DbContextOptionsBuilder();
       // public JsonItemContext CreateContext()
       // => new JsonItemContext((IConfiguration)options.UseSqlServer(ConnectionString));

       //public JsonItemRepository _jsonItemRepository => new JsonItemRepository(CreateContext());
       // public IUnitOfWork? unitOfWork;
       // public JsonItemDiffService JsonItemDiffService => new JsonItemDiffService(unitOfWork);

    }

}


